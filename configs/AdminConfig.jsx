// config/adminConfig.js
export const adminConfig = {
  emails: [
    "tusharrohilla1121@gmail.com",
    // Only the above email has admin access
  ],
  maxCoursesForFreeUser: 5, // Free users can create up to 5 courses
  makeEveryoneAdmin: false, // Only specified emails above have admin access
};
